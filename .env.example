# Local dev: sqlite:///./dev.db
# Production (Supabase): postgresql://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=sqlite:///./dev.db

# Random secret used to sign session cookies
SECRET_KEY=replace-with-a-random-secret-value
